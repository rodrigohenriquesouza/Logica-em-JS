// Criar um programa para o Usuário digitar dois números e calcular a média entre eles

var n1 = Number(prompt('Digite um número'))
var n2 = Number(prompt('Digite outro número'))

let media = (n1 + n2) / 2
alert(`A soma entre o número ${n1} e o número ${n2} tem a media ${media}`)